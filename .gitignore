# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Node
node_modules/
dist/

# IDE
.idea/
rebel.xml

# Agent worktrees
.worktrees/

# Data（运行时产物: 底库 pickle / 头像 / 抓拍）
face_db/

# 模型: 保留 models/buffalo_s/*.onnx 随仓库提供, 但禁止误提交下载的 zip 包
models/*.zip
models/**/*.zip
# buffalo_l 识别器(w600k_r50.onnx 166MB)超 GitHub 单文件限制,不随仓库提供,需自行下载解压到 models/buffalo_l/
models/buffalo_l/

# Environment（含数据库密码，不要提交）
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# InsightFace 按官方布局自动下载的模型缓存(backend/models/,约 280MB,含 zip 包),禁止入库
backend/models/

# 临时/工具产物（不入库）
.reasonix/
_worker_stage/
ai monitor/

# pose_plugin 拷贝的姿态项目源码：排除大件/环境/缓存
pose_plugin/.venv-worker/
pose_plugin/models/*.pt
# TensorRT 引擎(机器特定产物)与 ONNX 导出中间产物, 均不入库
pose_plugin/models/*.onnx
pose_plugin/models/*.engine
pose_plugin/fixtures/*.mp4
pose_plugin/**/__pycache__/
pose_plugin/.pytest_cache/
pose_plugin/*.egg-info/
pose_plugin/**/*.pyc
pose_plugin/**/*.pyo
# 姿态 Worker 运行时产物(事件归档 SQLite, 不随仓库分发)
pose_plugin/var/

# 根目录临时融合文档与调试脚本目录(不入库)
融合实施_work/
修改文档.md
修改文档.txt
融合实施.md
融合实施_HANDOFF.md
